<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToolTester.Presentation.Pages.MainPage"
             x:DataType="PageModels:MainPageModel"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"         
             xmlns:PageModels="clr-namespace:ToolTester.Presentation.PageModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"           
             xmlns:syncfusion="http://schemas.syncfusion.com/maui/toolkit">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
              EventName="NavigatedTo"
              Command="{Binding NavigatedToCommand}" />
        <toolkit:EventToCommandBehavior
              EventName="NavigatedFrom"
              Command="{Binding NavigatedFromCommand}" />
        <toolkit:EventToCommandBehavior
              EventName="Appearing"                
              Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    

    <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
                  
                    <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

                    <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />
            
        <Button
                x:Name="Relationshiplink"
                Text="Relationships" 
                SemanticProperties.Hint="Counts the number of times you click"                  
                Command="{Binding NavigateToCatalogPageCommand}" 
                HorizontalOptions="Fill" />
            
        <Button
                x:Name="JulietLink"
                Text="Juliets" 
                SemanticProperties.Hint="Counts the number of times you click"                  
                Command="{Binding NavigateToJulietPageCommand}" 
                HorizontalOptions="Fill" />
        <Button
        x:Name="ParseLink"
        Text="Parse" 
        SemanticProperties.Hint="Counts the number of times you click"                  
        Command="{Binding NavigateToParsePageCommand}" 
        HorizontalOptions="Fill" />
          <Button
  x:Name="ReportLink"
  Text="Report" 
  SemanticProperties.Hint="Counts the number of times you click"                  
  Command="{Binding NavigateToReportPageCommand}" 
  HorizontalOptions="Fill" />
    </VerticalStackLayout>

</ContentPage>
